
const colors = require('colors/safe');
const util = require('util');
const dump = function(info){
    // if(typeof info )
};

console.log(colors.green(util.inspect({"a":111})))

// module.exports = {
//     dump  
// }